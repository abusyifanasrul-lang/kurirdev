name: Supabase Daily Backup

on:
  schedule:
    - cron: '0 17 * * *'
  workflow_dispatch: 

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install PostgreSQL Client
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client

      - name: Dump Database
        run: |
          # Menggunakan koneksi pooling port 6543
          pg_dump "${{ secrets.DB_URL }}" > backup.sql
        env:
          PGSSLMODE: 'require' # Supabase pooling mewajibkan SSL aktif
