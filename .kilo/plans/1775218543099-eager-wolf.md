# KurirDev — Detailed Implementation Plan

> Rencana implementasi terperinci berdasarkan `KurirDev_MasterAnalysis.md`. Setiap langkah mencakup file yang diubah, kode sebelum/sesudah, dependency, testing strategy, dan acceptance criteria.

---

## 📋 Daftar Isi

1. [Phase 1 — SEGERA (Security & Data Integrity)](#phase-1)
2. [Phase 2 — Reliabilitas (Error Handling & Offline)](#phase-2)
3. [Phase 3 — Maintainability (Refactoring & Cleanup)](#phase-3)
4. [Phase 4 — Polish (Optimasi)](#phase-4)
5. [Testing Strategy](#testing-strategy)
6. [Rollback Plan](#rollback-plan)

---

## Phase 1 — SEGERA (Security & Data Integrity)

### 1.1 Hapus Password dari User Interface & Client-Side Storage

**Estimasi:** 3 jam | **Risiko:** Medium | **Dependencies:** Tidak ada

#### File yang Diubah

| File | Aksi |
|------|------|
| `src/types/index.ts` | Hapus field `password?` dari interface `User` |
| `src/stores/useUserStore.ts` | Buat type `CreateUserInput` terpisah, update `addUser` |
| `src/stores/useCourierStore.ts` | Hapus `INITIAL_QUEUE`, hapus `password` dari Courier interface |
| `src/pages/courier/CourierProfile.tsx` | **REWRITE fitur ganti password** — gunakan Supabase Auth API, bukan client-side validation |
| `src/pages/Login.tsx` | Hapus info default password dari UI, update untuk tidak membaca password dari state |
| `src/stores/useSessionStore.ts` | **VERIFIKASI** — pastikan tidak menyimpan data sensitif di localStorage |
| `src/stores/useSessionStore.ts` | **VERIFIKASI** — pastikan tidak menyimpan data sensitif di localStorage |

#### Langkah Detail

**Step 1: Buat type `CreateUserInput` di `src/types/index.ts`**
```typescript
// TAMBAHKAN setelah interface User
export interface CreateUserInput {
  name: string;
  email: string;
  password: string;  // Hanya untuk pembuatan user
  role: UserRole;
  phone?: string;
}
```

**Step 2: Hapus `password?` dari `User` interface di `src/types/index.ts:35`**
```typescript
// SEBELUM
export interface User {
  id: string;
  name: string;
  email: string;
  role: UserRole;
  password?: string;  // HAPUS BARIS INI
  phone?: string;
  ...
}

// SESUDAH
export interface User {
  id: string;
  name: string;
  email: string;
  role: UserRole;
  phone?: string;
  ...
}
```

**Step 3: Update `useUserStore.ts` — ganti validasi password**
```typescript
// DI fungsi addUser — ubah parameter dan validasi
addUser: async (input: CreateUserInput) => {
  // SEBELUM: if (!user.name || !user.email || !user.password || !user.role)
  // SESUDAH:
  if (!input.name || !input.email || !input.password || !input.role) {
    throw new Error('Data tidak lengkap')
  }
  // Password hanya dikirim ke edge function, TIDAK disimpan di state
  const { data, error } = await supabase.functions.invoke('create-staff-user', {
    body: {
      email: input.email,
      password: input.password,  // Langsung ke edge function, tidak lewat state
      ...
    }
  })
}
```

**Step 4: Hapus `INITIAL_QUEUE` dari `useCourierStore.ts`**
```typescript
// HAPUS seluruh blok INITIAL_QUEUE (baris 23-66)
// Ganti dengan fetch dari Supabase saat store diinisialisasi
```

**Step 5: Hapus `password` dari Courier interface**
```typescript
// DI src/types/index.ts — Courier interface
export interface Courier extends Omit<User, 'password'> {
  vehicle_type?: 'motorcycle' | 'car' | 'bicycle' | 'van';
  plate_number?: string;
}
```

**Step 6: REWRITE fitur Ganti Password di `CourierProfile.tsx`**

Saat ini fitur ganti password SANGAT BERBAHAYA — validasi password dilakukan di client-side (`user?.password !== passwordForm.currentPassword` di baris 75) dan password baru disimpan ke state (bukan ke Supabase Auth). Ini berarti:
1. Password lama divalidasi dari state client (bisa dimanipulasi)
2. Password baru tidak pernah diupdate di Supabase Auth
3. Password tersimpan di Zustand state yang bisa dilihat di DevTools

```typescript
// SESUDAH — gunakan Supabase Auth API untuk ganti password
import { supabase } from '@/lib/supabaseClient'

const handleChangePassword = async () => {
  if (passwordForm.newPassword !== passwordForm.confirmPassword) {
    setMessage({ type: 'error', text: 'Password tidak cocok' })
    return
  }
  if (passwordForm.newPassword.length < 8) {
    setMessage({ type: 'error', text: 'Password minimal 8 karakter' })
    return
  }

  setIsLoading(true)
  
  // Update password via Supabase Auth — BUKAN via state
  const { error } = await supabase.auth.updateUser({
    password: passwordForm.newPassword
  })
  
  if (error) {
    setMessage({ type: 'error', text: error.message })
  } else {
    setMessage({ type: 'success', text: 'Password berhasil diperbarui!' })
    setPasswordForm({ currentPassword: '', newPassword: '', confirmPassword: '' })
    setIsChangePasswordOpen(false)
  }
  
  setIsLoading(false)
  setTimeout(() => setMessage(null), 5000)
}
```

**Step 7: Hapus info default password dari UI Login**
```typescript
// HAPUS baris 279 di Login.tsx:
// <p className="text-xs text-center text-gray-400 mt-2">Password default: *[nama_role]123*</p>

// Atau ganti dengan placeholder yang tidak menampilkan password real:
// <p className="text-xs text-center text-gray-400 mt-2">Hubungi admin untuk credential</p>
```

**Step 8: Verifikasi `useSessionStore.ts` tidak menyimpan data sensitif**
```typescript
// HAPUS baris 279 di Login.tsx:
// <p className="text-xs text-center text-gray-400 mt-2">Password default: *[nama_role]123*</p>

// Atau ganti dengan placeholder yang tidak menampilkan password real:
// <p className="text-xs text-center text-gray-400 mt-2">Hubungi admin untuk credential</p>
```

#### Testing
- [ ] TypeScript compile tanpa error setelah hapus `password`
- [ ] Login masih berfungsi dengan Supabase Auth
- [ ] Create user baru berhasil (password dikirim ke edge function, tidak ke state)
- [ ] Tidak ada password di DevTools > Application > LocalStorage

#### Acceptance Criteria
- ✅ `password` tidak ada di `User` interface
- ✅ Tidak ada password di localStorage atau Zustand state
- ✅ `INITIAL_QUEUE` dihapus
- ✅ User creation masih berfungsi via edge function
- ✅ Ganti password menggunakan Supabase Auth API (bukan client-side)
- ✅ Tidak ada info default password di UI Login
- ✅ CourierProfile tidak lagi mengakses `user.password`

---

### 1.2 Fix User Suspend Masih Bisa Login

**Estimasi:** 15 menit | **Risiko:** Low | **Dependencies:** 1.1 (opsional)

#### File yang Diubah

| File | Aksi |
|------|------|
| `src/pages/Login.tsx` | Cek `profile.is_active` sebelum login success |

#### Langkah Detail

**Step 1: Tambahkan cek `is_active` di `Login.tsx` setelah fetch profile**

Cari baris ~69-77 (setelah `if (profileError || !profile)`):
```typescript
// SEBELUM
if (profileError || !profile) {
  throw new Error('Data pengguna tidak ditemukan di database.');
}
// Langsung lanjut ke userData construction...

// SESUDAH — tambahkan setelah cek profile
if (profileError || !profile) {
  throw new Error('Data pengguna tidak ditemukan di database.');
}
if (!profile.is_active) {
  throw new Error('Akun Anda telah dinonaktifkan. Hubungi administrator.');
}
```

**Step 2: Gunakan nilai `is_active` dari database, bukan hardcoded**
```typescript
// SEBELUM (baris ~85)
const userData: UserType = {
  ...
  is_active: true,  // HARDCODED
  ...
};

// SESUDAH
const userData: UserType = {
  ...
  is_active: profile.is_active ?? true,  // DARI DATABASE
  ...
};
```

#### Testing
- [ ] Set `is_active = false` di Supabase untuk user test
- [ ] Login dengan user tersebut → harus ditolak dengan pesan error
- [ ] Set `is_active = true` → login berhasil

#### Acceptance Criteria
- ✅ User dengan `is_active = false` tidak bisa login
- ✅ Pesan error jelas: "Akun Anda telah dinonaktifkan"
- ✅ User aktif masih bisa login normal

---

### 1.3 Ganti `localStorage.clear()` dengan Selective Removal

**Estimasi:** 30 menit | **Risiko:** Low | **Dependencies:** Tidak ada

#### File yang Diubah

| File | Aksi |
|------|------|
| `src/context/AuthContext.tsx` | Ganti `localStorage.clear()` dengan selective removal |

#### Langkah Detail

**Step 1: Ganti `localStorage.clear()` di `AuthContext.tsx:144`**
```typescript
// SEBELUM (baris ~144)
try { localStorage.clear(); } catch(e) {}

// SESUDAH
try {
  const keysToRemove = [
    'session-storage',
    'courier-storage',
    'settings-storage',
    'kurirdev_db_meta',
    'pwa_update_dismissed',
  ];
  keysToRemove.forEach(key => localStorage.removeItem(key));
} catch(e) {}
```

**Catatan:** `lastLoginEmail` TIDAK dihapus agar fitur "Remember Me" tetap berfungsi.

#### Testing
- [ ] Logout → cek localStorage di DevTools, hanya key KurirDev yang terhapus
- [ ] Login kembali → email masih terisi di form login (remember me)
- [ ] Tidak ada error setelah logout

#### Acceptance Criteria
- ✅ `localStorage.clear()` tidak lagi digunakan
- ✅ Hanya key KurirDev yang dihapus saat logout
- ✅ `lastLoginEmail` tetap ada setelah logout

---

### 1.4 Fix ID Order Duplikat

**Estimasi:** 2 jam | **Risiko:** Medium | **Dependencies:** Tidak ada

#### File yang Diubah

| File | Aksi |
|------|------|
| `src/stores/useOrderStore.ts` | Fix `generateOrderId` untuk query Supabase |
| Database (SQL) | Tambah UNIQUE constraint pada `order_number` |

#### Langkah Detail

**Step 1: Tambah UNIQUE constraint di database**

Buat migration SQL:
```sql
-- File: supabase/migrations/001_unique_order_number.sql
ALTER TABLE orders ADD CONSTRAINT unique_order_number UNIQUE (order_number);
```

**Step 2: Rewrite `generateOrderId` di `useOrderStore.ts:464-472`**

Opsi A — Query Supabase untuk count:
```typescript
// SESUDAH — query Supabase untuk count hari ini
generateOrderId: async () => {
  const now = new Date()
  const DD = String(now.getDate()).padStart(2, '0')
  const MM = String(now.getMonth() + 1).padStart(2, '0')
  const YY = String(now.getFullYear()).slice(-2)
  const dateKey = `${DD}${MM}${YY}`
  const prefix = `P${dateKey}`

  // Query Supabase untuk count order hari ini
  const startOfDay = new Date(now.getFullYear(), now.getMonth(), now.getDate()).toISOString()
  const endOfDay = new Date(now.getFullYear(), now.getMonth(), now.getDate() + 1).toISOString()

  const { count, error } = await supabase
    .from('orders')
    .select('*', { count: 'exact', head: true })
    .gte('created_at', startOfDay)
    .lte('created_at', endOfDay)

  if (error) {
    // Fallback: gunakan timestamp jika query gagal
    const fallback = `${prefix}${String(Date.now()).slice(-3)}`
    console.warn('Failed to count orders, using fallback:', fallback)
    return fallback
  }

  return `${prefix}${String((count || 0) + 1).padStart(3, '0')}`
},
```

Opsi B — Gunakan UUID + nomor order dari database trigger (lebih robust, tapi butuh setup):
```sql
-- Database function untuk generate order number
CREATE OR REPLACE FUNCTION generate_order_number()
RETURNS TEXT AS $$
DECLARE
  date_key TEXT;
  seq_num INTEGER;
BEGIN
  date_key := TO_CHAR(NOW(), 'DDMMYY');
  SELECT COALESCE(MAX(CAST(SUBSTRING(order_number FROM 9) AS INTEGER)), 0) + 1
  INTO seq_num
  FROM orders
  WHERE order_number LIKE 'P' || date_key || '%';
  RETURN 'P' || date_key || LPAD(seq_num::TEXT, 3, '0');
END;
$$ LANGUAGE plpgsql;
```

**Rekomendasi:** Mulai dengan Opsi A (lebih cepat), migrasi ke Opsi B saat scale > 500 order/hari.

**Step 3: Update pemanggilan `generateOrderId`**

Karena sekarang async, update semua tempat yang memanggilnya:
```typescript
// SEBELUM
const orderNumber = get().generateOrderId()

// SESUDAH
const orderNumber = await get().generateOrderId()
```

#### Testing
- [ ] Buat 2 order bersamaan dari 2 browser berbeda → ID tidak duplikat
- [ ] Refresh browser → counter tidak reset
- [ ] UNIQUE constraint mencegah duplikat jika race condition tetap terjadi

#### Acceptance Criteria
- ✅ `generateOrderId` query Supabase, bukan memori lokal
- ✅ UNIQUE constraint di database sebagai safety net
- ✅ Fallback mechanism jika query gagal

---

### 1.6 Bersihkan Console.log dan Info Leak di AuthContext

**Estimasi:** 30 menit | **Risiko:** Low | **Dependencies:** Tidak ada

#### File yang Diubah

| File | Aksi |
|------|------|
| `src/context/AuthContext.tsx` | Hapus console.log yang tidak perlu |
| `src/lib/logger.ts` | Buat utility logger (jika belum ada) |

#### Langkah Detail

**Step 1: Hapus/replace console.log di `AuthContext.tsx`**

Baris yang perlu dibersihkan:
- Baris 67: `console.error('Profile not found for:', userId)` → gunakan logger atau hapus
- Baris 71: `console.error('Error fetching profile from Supabase:', error)` → gunakan logger
- Baris 80: `console.error('Supabase session error:', error)` → gunakan logger
- Baris 95: `console.log('Supabase Auth Event:', event)` → HAPUS (info leak)
- Baris 107: `console.log('Session already active for user:', session.user.id)` → HAPUS (info leak)
- Baris 122: `console.log('Initiating logout...')` → HAPUS
- Baris 126: `console.error('Error during Supabase sign out:', err)` → gunakan logger
- Baris 147: `console.log('Logout cleanup complete.')` → HAPUS

```typescript
// Buat logger utility sederhana
// src/lib/logger.ts
const isDev = import.meta.env.DEV
export const logger = {
  debug: (...args: unknown[]) => isDev && console.log('[DEBUG]', ...args),
  info: (...args: unknown[]) => isDev && console.info('[INFO]', ...args),
  warn: (...args: unknown[]) => console.warn('[WARN]', ...args),
  error: (...args: unknown[]) => console.error('[ERROR]', ...args),
}
```

**Step 2: Replace semua console.log/error di AuthContext**
```typescript
// Ganti:
console.error('Error fetching profile from Supabase:', error)
// Dengan:
import { logger } from '@/lib/logger'
logger.error('Error fetching profile:', error)
```

#### Acceptance Criteria
- ✅ Tidak ada `console.log` yang menampilkan info sensitif di production
- ✅ Error logging tetap ada untuk debugging (via logger utility)
- ✅ Auth event logs tidak tampil di production

**Estimasi:** 1.5 jam | **Risiko:** Low | **Dependencies:** Tidak ada

#### File yang Diubah

| File | Aksi |
|------|------|
| `src/services/notificationService.ts` | Rewrite — ganti fetch `/api/send-notification` dengan Supabase Edge Function |
| `.env` | Pastikan tidak ada `VITE_API_SECRET` (sudah dikonfirmasi tidak ada) |
| `src/utils/notification.ts` | Cek apakah ada yang import dari notificationService |

#### Langkah Detail

**Step 1: Rewrite `notificationService.ts` — ganti REST call dengan Edge Function**

File saat ini (38 baris) menggunakan `fetch('/api/send-notification')` ke endpoint yang TIDAK ADA, ditambah header `x-api-secret` yang menggunakan `VITE_API_SECRET` (juga tidak ada di `.env`). Ini berarti notifikasi push TIDAK PERNAH berfungsi.

```typescript
// SESUDAH — gunakan Supabase Edge Function
import { supabase } from '@/lib/supabaseClient'

interface SendPushParams {
    token: string
    title: string
    body: string
    data?: Record<string, string>
}

export const sendPushNotification = async (params: SendPushParams): Promise<boolean> => {
    try {
        const { data, error } = await supabase.functions.invoke('notify-courier', {
            body: {
                fcm_token: params.token,
                title: params.title,
                body: params.body,
                data: params.data,
            },
        })

        if (error) {
            console.error('Push notification failed:', error)
            return false
        }

        return true
    } catch (error) {
        console.error('Push notification error:', error)
        return false
    }
}
```

**Step 2: Cek semua pemanggilan `sendPushNotification`**

Cari semua tempat yang memanggil fungsi ini dan pastikan parameter yang dikirim sesuai dengan signature baru.

**Step 3: Verifikasi Edge Function `notify-courier`**

Pastikan `supabase/functions/notify-courier/index.ts`:
- Menerima parameter yang benar (fcm_token, title, body, data)
- Validasi autentikasi user (hanya authenticated users)
- Validasi role (hanya admin/admin_kurir yang bisa kirim)
- Menggunakan Firebase Admin SDK untuk kirim FCM

#### Testing
- [ ] Kirim notifikasi dari admin → kurir menerima
- [ ] Coba kirim notifikasi sebagai kurir → ditolak
- [ ] Tidak ada secret di client bundle

#### Acceptance Criteria
- ✅ `VITE_API_SECRET` tidak digunakan di client
- ✅ Notifikasi via Supabase Edge Function
- ✅ Validasi role di server-side

---

## Phase 2 — Reliabilitas (Error Handling & Offline)

### 2.1 Buat Error Boundary Components

**Estimasi:** 2 jam | **Risiko:** Low | **Dependencies:** Tidak ada

#### File yang Dibuat/Diubah

| File | Aksi |
|------|------|
| `src/components/ErrorBoundary.tsx` | Buat baru — Error Boundary utama |
| `src/components/RouteErrorBoundary.tsx` | Buat baru — Per-route isolation |
| `src/components/ChartErrorBoundary.tsx` | Buat baru — Per-chart isolation |
| `src/App.tsx` | Wrap routes dengan ErrorBoundary |

#### Langkah Detail

**Step 1: Buat `ErrorBoundary.tsx`**
```typescript
import { Component, type ErrorInfo, type ReactNode } from 'react'

interface Props {
  children: ReactNode
  fallback?: ReactNode
  onError?: (error: Error, info: ErrorInfo) => void
}

interface State {
  hasError: boolean
  error: Error | null
}

export class ErrorBoundary extends Component<Props, State> {
  constructor(props: Props) {
    super(props)
    this.state = { hasError: false, error: null }
  }

  static getDerivedStateFromError(error: Error): State {
    return { hasError: true, error }
  }

  componentDidCatch(error: Error, info: ErrorInfo) {
    console.error('ErrorBoundary caught:', error, info)
    this.props.onError?.(error, info)
  }

  render() {
    if (this.state.hasError) {
      return this.props.fallback || (
        <div className="flex flex-col items-center justify-center min-h-[200px] p-6 text-center">
          <h2 className="text-xl font-semibold text-red-600 mb-2">Terjadi Kesalahan</h2>
          <p className="text-gray-600 mb-4">Komponen ini mengalami error.</p>
          <button
            onClick={() => this.setState({ hasError: false, error: null })}
            className="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700"
          >
            Coba Lagi
          </button>
          {import.meta.env.DEV && this.state.error && (
            <pre className="mt-4 p-3 bg-gray-100 rounded text-sm text-left overflow-auto max-w-full">
              {this.state.error.message}
            </pre>
          )}
        </div>
      )
    }
    return this.props.children
  }
}
```

**Step 2: Wrap Root App di `App.tsx`**
```typescript
// Wrap seluruh App component dengan ErrorBoundary
<ErrorBoundary>
  <Router>
    {/* routes */}
  </Router>
</ErrorBoundary>
```

**Step 3: Wrap setiap lazy-loaded route**
```typescript
// Contoh di App.tsx
<Route
  path="/orders"
  element={
    <RouteErrorBoundary pageName="Orders">
      <Orders />
    </RouteErrorBoundary>
  }
/>
```

**Step 4: Wrap chart components di Dashboard/Reports**
```typescript
<ErrorBoundary fallback={<div className="p-4 text-center text-gray-500">Chart tidak dapat dimuat</div>}>
  <RechartsComponent />
</ErrorBoundary>
```

#### Testing
- [ ] Throw error di komponen test → ErrorBoundary menampilkan fallback
- [ ] Error di satu route tidak crash route lain
- [ ] Error di chart tidak crash halaman
- [ ] Tombol "Coba Lagi" berfungsi

#### Acceptance Criteria
- ✅ Error Boundary di 3 level: root, route, chart
- ✅ Fallback UI yang informatif
- ✅ Dev mode menampilkan detail error
- ✅ Recovery button berfungsi

---

### 2.2 Buat Retry Utility

**Estimasi:** 2 jam | **Risiko:** Low | **Dependencies:** 2.1 (opsional)

#### File yang Dibuat/Diubah

| File | Aksi |
|------|------|
| `src/lib/retry.ts` | Buat baru — retry utility dengan exponential backoff |
| `src/stores/useOrderStore.ts` | Wrap semua Supabase writes dengan retry |
| `src/stores/useUserStore.ts` | Wrap Supabase writes dengan retry |
| `src/stores/useCourierStore.ts` | Wrap Supabase writes dengan retry |

#### Langkah Detail

**Step 1: Buat `retry.ts`**
```typescript
export class RetryError extends Error {
  constructor(
    message: string,
    public readonly attempts: number,
    public readonly lastError: Error
  ) {
    super(message)
    this.name = 'RetryError'
  }
}

export interface RetryOptions {
  maxRetries?: number
  baseDelay?: number
  maxDelay?: number
  onRetry?: (attempt: number, error: Error) => void
}

const DEFAULT_OPTIONS: Required<RetryOptions> = {
  maxRetries: 3,
  baseDelay: 1000,
  maxDelay: 10000,
  onRetry: () => {},
}

export async function withRetry<T>(
  fn: () => Promise<T>,
  options: RetryOptions = {}
): Promise<T> {
  const { maxRetries, baseDelay, maxDelay, onRetry } = { ...DEFAULT_OPTIONS, ...options }
  let lastError: Error | null = null

  for (let attempt = 0; attempt <= maxRetries; attempt++) {
    try {
      return await fn()
    } catch (error) {
      lastError = error instanceof Error ? error : new Error(String(error))
      onRetry(attempt + 1, lastError)

      if (attempt === maxRetries) {
        throw new RetryError(
          `Failed after ${maxRetries + 1} attempts: ${lastError.message}`,
          maxRetries + 1,
          lastError
        )
      }

      const delay = Math.min(baseDelay * Math.pow(2, attempt), maxDelay)
      await new Promise(resolve => setTimeout(resolve, delay))
    }
  }

  throw lastError // Should never reach here
}

// Helper untuk cek apakah error bisa di-retry
export function isRetryable(error: Error): boolean {
  const message = error.message.toLowerCase()
  // Jangan retry error yang tidak akan berubah dengan retry
  const nonRetryable = ['unauthorized', 'forbidden', 'not found', 'invalid', 'duplicate', 'unique']
  return !nonRetryable.some(msg => message.includes(msg))
}
```

**Step 2: Wrap Supabase writes di `useOrderStore.ts`**
```typescript
import { withRetry, isRetryable } from '@/lib/retry'

// CONTOH: addOrder
addOrder: async (order: Order) => {
  const { error } = await withRetry(
    () => supabase.from('orders').insert(order).then(({ data, error }) => {
      if (error) throw error
      return data
    }),
    { maxRetries: 3, onRetry: (n, e) => console.warn(`Retry ${n}:`, e.message) }
  )
  // ...
}

// CONTOH: complete_order RPC
updateOrderStatus: async (orderId, status, userId, userName, notes) => {
  if (status === 'delivered') {
    const { error } = await withRetry(
      () => supabase.rpc('complete_order', {
        p_order_id: orderId,
        // ...
      }).then(({ error }) => { if (error) throw error })
    )
    if (error) throw error
  }
  // ...
}
```

**Step 3: Wrap silent-fail functions**
```typescript
// SEBELUM — silent fail
updateBiayaTambahan: async (orderId, titik, beban) => {
  await supabase.from('orders').update({...}).eq('id', orderId)
  // No error handling!
}

// SESUDAH — proper error handling
updateBiayaTambahan: async (orderId, titik, beban) => {
  const { error } = await withRetry(
    () => supabase.from('orders').update({
      total_biaya_titik: titik,
      total_biaya_beban: beban,
    }).eq('id', orderId).then(({ error }) => { if (error) throw error })
  )
  if (error) throw error
}
```

#### Testing
- [ ] Simulasi network error → retry 3x dengan delay exponential
- [ ] Error non-retryable (401, 403) → langsung throw tanpa retry
- [ ] Retry berhasil di attempt ke-2 → function return normal

#### Acceptance Criteria
- ✅ `withRetry` utility berfungsi dengan exponential backoff
- ✅ Semua Supabase writes menggunakan retry
- ✅ Tidak ada silent failures
- ✅ `isRetryable` mencegah retry pada error yang tidak akan berubah

---

### 2.3 Fix Race Condition di AppListeners

**Estimasi:** 2 jam | **Risiko:** Low | **Dependencies:** 2.2

#### File yang Diubah

| File | Aksi |
|------|------|
| `src/components/AppListeners.tsx` | Tambah debounce, cleanup, error handling |

#### Langkah Detail

**Step 1: Tambah debounce pada subscription callbacks**
```typescript
import { useRef, useCallback } from 'react'

// DI DALAM AppListeners component
const debounceRef = useRef<Record<string, ReturnType<typeof setTimeout>>>({})

const debouncedFetchProfile = useCallback((userId: string) => {
  if (debounceRef.current['profile']) {
    clearTimeout(debounceRef.current['profile'])
  }
  debounceRef.current['profile'] = setTimeout(() => {
    fetchProfile(userId)
  }, 500)
}, [fetchProfile])

const debouncedFetchSettings = useCallback(() => {
  if (debounceRef.current['settings']) {
    clearTimeout(debounceRef.current['settings'])
  }
  debounceRef.current['settings'] = setTimeout(() => {
    fetchSettings()
  }, 500)
}, [fetchSettings])
```

**Step 2: Fix setTimeout cleanup**
```typescript
// SEBELUM
setTimeout(async () => {
  if (!isInitialSyncCompleted(userId)) {
    await syncAllFinalOrders(fetchFn, userId)
  }
  // ...
}, 1000)

// SESUDAH
useEffect(() => {
  let cancelled = false
  const timer = setTimeout(async () => {
    if (cancelled) return
    try {
      if (!isInitialSyncCompleted(userId)) {
        await withRetry(() => syncAllFinalOrders(fetchFn, userId))
      } else if (needsDeltaSync(userId)) {
        await withRetry(() => deltaSyncYesterday(fetchFn, userId))
      }
      await checkIntegrity()
    } catch (error) {
      console.error('Background sync failed:', error)
    }
  }, 1000)

  return () => {
    cancelled = true
    clearTimeout(timer)
  }
}, [userId])
```

**Step 3: Tambah subscription error handling**
```typescript
const profileChannel = supabase
  .channel(`public:profiles:id=eq.${user.id}`)
  .on('postgres_changes', { ... }, () => { debouncedFetchProfile(user.id) })
  .subscribe((status, error) => {
    if (status === 'CHANNEL_ERROR' || status === 'TIMED_OUT') {
      console.error('Profile subscription error:', error)
      // Fallback: fetch langsung
      fetchProfile(user.id)
    }
  })
```

**Step 4: Fix missing useEffect dependencies**
```typescript
// SEBELUM
}, [user?.id])

// SESUDAH
}, [user?.id, fetchProfile, fetchSettings])
```

#### Testing
- [ ] Rapid database updates → fetchProfile hanya dipanggil sekali
- [ ] Component unmount → setTimeout dibatalkan
- [ ] Subscription error → fallback fetch berjalan

#### Acceptance Criteria
- ✅ Debounce pada subscription callbacks (500ms)
- ✅ Cleanup pada unmount (no memory leak)
- ✅ Subscription error handling dengan fallback
- ✅ useEffect dependencies lengkap

---

### 2.4 Fix Data Kurir Ganda (Dual Source of Truth)

**Estimasi:** 5 jam | **Risiko:** High | **Dependencies:** 1.1

#### File yang Diubah

| File | Aksi |
|------|------|
| `src/stores/useCourierStore.ts` | Hapus `queue` array, hapus `persist`, jadikan view layer |
| `src/stores/useUserStore.ts` | Pastikan courier data dari sini |
| Database | Pastikan `queue_position` ada di `profiles` |

#### Masalah yang Ditemukan dari Pembacaan Langsung

**Masalah 1: `queue` array di-manipulasi di 5 fungsi berbeda**
- `addCourier` (baris 94): `set((state) => ({ queue: [...state.queue, courier] }))`
- `updateCourier` (baris 98-104): update queue lokal
- `removeCourier` (baris 113-115): filter queue lokal
- `rotateQueue` (baris 142-149): manipulasi queue lokal
- `reset` (baris 80-83): reset ke INITIAL_QUEUE

**Masalah 2: `persist` middleware menyimpan `queue` ke localStorage**
- Key: `courier-storage`
- Data yang dipersist: seluruh state termasuk `queue` (dengan password!)

**Masalah 3: `couriers` getter sudah benar — read dari `useUserStore`**
- Baris 76-78: `get couriers() { return useUserStore.getState().users.filter(u => u.role === 'courier') }`
- Ini sudah benar, tapi `queue` array TETAP ada dan dimanipulasi

**Masalah 4: `getAvailableCouriers` sudah benar — read dari `useUserStore`**
- Baris 119-123: sudah menggunakan `useUserStore.getState().users`

#### Langkah Detail

**Step 1: Hapus `queue` dan semua manipulasinya dari `useCourierStore`**

```typescript
// HAPUS:
// - INITIAL_QUEUE constant (baris 23-66)
// - queue: INITIAL_QUEUE dari state (baris 74)
// - persist middleware wrapper (baris 70-205)
// - Semua manipulasi queue di addCourier, updateCourier, removeCourier, rotateQueue
```

**Step 2: Rewrite `useCourierStore` tanpa persist dan tanpa queue**

```typescript
import { create } from 'zustand'
import { Courier } from '@/types'
import { useUserStore } from './useUserStore'
import { supabase } from '@/lib/supabaseClient'

interface CourierState {
  readonly couriers: Courier[]
  addCourier: (courier: Courier) => Promise<void>
  updateCourier: (id: string, data: Partial<Courier>) => Promise<void>
  updateCourierStatus: (id: string, data: Partial<Courier>) => Promise<void>
  removeCourier: (id: string) => Promise<void>
  getAvailableCouriers: () => Courier[]
  rotateQueue: (assignedCourierId: string) => Promise<void>
  setCourierOffline: (courierId: string, reason: string) => Promise<void>
  setCourierOnline: (courierId: string, status: 'on' | 'stay') => Promise<void>
  reset: () => void
}

export const useCourierStore = create<CourierState>()((set, get) => ({
  get couriers() {
    return useUserStore.getState().users.filter(u => u.role === 'courier') as Courier[]
  },

  reset: () => {
    // Tidak ada yang perlu direset — data dari Supabase
  },

  addCourier: async (courier) => {
    const result = await useUserStore.getState().addUser(courier)
    if (!result.success) {
      throw new Error(result.error || 'Gagal membuat akun kurir')
    }
  },

  updateCourier: async (id, data) => {
    await useUserStore.getState().updateUser(id, data)
  },

  updateCourierStatus: async (id, data) => {
    await get().updateCourier(id, data)
  },

  removeCourier: async (id) => {
    await useUserStore.getState().removeUser(id)
  },

  getAvailableCouriers: () => {
    const { users } = useUserStore.getState()
    return users.filter(u =>
      u.role === 'courier' && u.is_active && u.is_online
    ) as Courier[]
  },

  rotateQueue: async (assignedCourierId) => {
    const userStore = useUserStore.getState()
    const allCouriers = userStore.users.filter(u => u.role === 'courier') as (Courier & { queue_position?: number })[]
    if (allCouriers.length === 0) return
    const sorted = [...allCouriers].sort((a, b) =>
      (a.queue_position ?? 999) - (b.queue_position ?? 999)
    )
    const maxPosition = sorted.reduce((max, c) => Math.max(max, c.queue_position ?? 0), 0)
    const assignedCourier = sorted.find(c => c.id === assignedCourierId)
    if (!assignedCourier) return
    const assignedCurrentPos = assignedCourier.queue_position ?? 1
    const updatePromises = sorted
      .filter(c => c.id !== assignedCourierId && (c.queue_position ?? 999) > assignedCurrentPos)
      .map(c => userStore.updateUserQueuePosition(c.id, (c.queue_position ?? 999) - 1))
    updatePromises.push(userStore.updateUserQueuePosition(assignedCourierId, maxPosition))
    await Promise.all(updatePromises)
  },

  setCourierOffline: async (courierId, reason) => {
    const userStore = useUserStore.getState()
    const allCouriers = userStore.users.filter(u => u.role === 'courier') as (Courier & { queue_position?: number })[]
    const thisCourier = allCouriers.find(c => c.id === courierId)
    const currentPos = thisCourier?.queue_position ?? 0

    await userStore.updateUser(courierId, {
      is_online: false,
      courier_status: 'off',
      off_reason: reason,
      queue_position: null as any,
    })

    if (currentPos > 0) {
      const shiftPromises = allCouriers
        .filter(c => c.id !== courierId && (c.queue_position ?? 0) > currentPos)
        .map(c => userStore.updateUserQueuePosition(c.id, (c.queue_position ?? 0) - 1))
      await Promise.all(shiftPromises)
    }
  },

  setCourierOnline: async (courierId, status) => {
    const userStore = useUserStore.getState()
    const allCouriers = userStore.users.filter(u => u.role === 'courier') as (Courier & { queue_position?: number })[]
    const maxPos = allCouriers
      .filter(c => c.id !== courierId)
      .reduce((max, c) => Math.max(max, c.queue_position ?? 0), 0)

    await userStore.updateUser(courierId, {
      is_online: true,
      courier_status: status,
      off_reason: '',
    })

    await userStore.updateUserQueuePosition(courierId, maxPos + 1)
  },
}))
```

**Step 3: Pastikan `queue_position` di Supabase jadi single source of truth**

Cek schema Supabase:
```sql
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS queue_position INTEGER DEFAULT 0;
```

**Step 4: Hapus `courier-storage` dari selective removal keys di AuthContext**

Karena tidak ada lagi `persist` di courier store, key `courier-storage` tidak perlu dihapus saat logout.

**Step 5: Cek semua referensi ke `useCourierStore.getState().queue`**

Cari semua tempat yang mengakses `queue` dan ganti dengan `couriers` getter.

**Step 2: Rewrite `useCourierStore` tanpa persist dan tanpa queue**

```typescript
import { create } from 'zustand'
import { Courier } from '@/types'
import { useUserStore } from './useUserStore'
import { supabase } from '@/lib/supabaseClient'

interface CourierState {
  readonly couriers: Courier[]
  addCourier: (courier: Courier) => Promise<void>
  updateCourier: (id: string, data: Partial<Courier>) => Promise<void>
  updateCourierStatus: (id: string, data: Partial<Courier>) => Promise<void>
  removeCourier: (id: string) => Promise<void>
  getAvailableCouriers: () => Courier[]
  rotateQueue: (assignedCourierId: string) => Promise<void>
  setCourierOffline: (courierId: string, reason: string) => Promise<void>
  setCourierOnline: (courierId: string, status: 'on' | 'stay') => Promise<void>
  reset: () => void
}

export const useCourierStore = create<CourierState>()((set, get) => ({
  get couriers() {
    return useUserStore.getState().users.filter(u => u.role === 'courier') as Courier[]
  },

  reset: () => {
    // Tidak ada yang perlu direset — data dari Supabase
  },

  addCourier: async (courier) => {
    const result = await useUserStore.getState().addUser(courier)
    if (!result.success) {
      throw new Error(result.error || 'Gagal membuat akun kurir')
    }
  },

  updateCourier: async (id, data) => {
    await useUserStore.getState().updateUser(id, data)
  },

  updateCourierStatus: async (id, data) => {
    await get().updateCourier(id, data)
  },

  removeCourier: async (id) => {
    await useUserStore.getState().removeUser(id)
  },

  getAvailableCouriers: () => {
    const { users } = useUserStore.getState()
    return users.filter(u =>
      u.role === 'courier' && u.is_active && u.is_online
    ) as Courier[]
  },

  rotateQueue: async (assignedCourierId) => {
    const userStore = useUserStore.getState()
    const allCouriers = userStore.users.filter(u => u.role === 'courier') as (Courier & { queue_position?: number })[]
    if (allCouriers.length === 0) return
    const sorted = [...allCouriers].sort((a, b) =>
      (a.queue_position ?? 999) - (b.queue_position ?? 999)
    )
    const maxPosition = sorted.reduce((max, c) => Math.max(max, c.queue_position ?? 0), 0)
    const assignedCourier = sorted.find(c => c.id === assignedCourierId)
    if (!assignedCourier) return
    const assignedCurrentPos = assignedCourier.queue_position ?? 1
    const updatePromises = sorted
      .filter(c => c.id !== assignedCourierId && (c.queue_position ?? 999) > assignedCurrentPos)
      .map(c => userStore.updateUserQueuePosition(c.id, (c.queue_position ?? 999) - 1))
    updatePromises.push(userStore.updateUserQueuePosition(assignedCourierId, maxPosition))
    await Promise.all(updatePromises)
  },

  setCourierOffline: async (courierId, reason) => {
    const userStore = useUserStore.getState()
    const allCouriers = userStore.users.filter(u => u.role === 'courier') as (Courier & { queue_position?: number })[]
    const thisCourier = allCouriers.find(c => c.id === courierId)
    const currentPos = thisCourier?.queue_position ?? 0

    await userStore.updateUser(courierId, {
      is_online: false,
      courier_status: 'off',
      off_reason: reason,
      queue_position: null as any,
    })

    if (currentPos > 0) {
      const shiftPromises = allCouriers
        .filter(c => c.id !== courierId && (c.queue_position ?? 0) > currentPos)
        .map(c => userStore.updateUserQueuePosition(c.id, (c.queue_position ?? 0) - 1))
      await Promise.all(shiftPromises)
    }
  },

  setCourierOnline: async (courierId, status) => {
    const userStore = useUserStore.getState()
    const allCouriers = userStore.users.filter(u => u.role === 'courier') as (Courier & { queue_position?: number })[]
    const maxPos = allCouriers
      .filter(c => c.id !== courierId)
      .reduce((max, c) => Math.max(max, c.queue_position ?? 0), 0)

    await userStore.updateUser(courierId, {
      is_online: true,
      courier_status: status,
      off_reason: '',
    })

    await userStore.updateUserQueuePosition(courierId, maxPos + 1)
  },
}))
```

**Step 3: Pastikan `queue_position` di Supabase jadi single source of truth**

Cek schema Supabase:
```sql
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS queue_position INTEGER DEFAULT 0;
```

**Step 4: Hapus `courier-storage` dari selective removal keys di AuthContext**

Karena tidak ada lagi `persist` di courier store, key `courier-storage` tidak perlu dihapus saat logout.

**Step 5: Cek semua referensi ke `useCourierStore.getState().queue`**

Cari semua tempat yang mengakses `queue` dan ganti dengan `couriers` getter.

#### Testing
- [ ] Hanya ada 1 sumber data kurir (Supabase via useUserStore)
- [ ] localStorage tidak menyimpan data kurir
- [ ] FIFO queue masih berfungsi dengan `queue_position`

#### Acceptance Criteria
- ✅ Tidak ada `queue` array di courier store
- ✅ Tidak ada `persist` middleware di courier store
- ✅ Semua data kurir dari `useUserStore` → Supabase
- ✅ `queue_position` di database jadi single source of truth

---

### 2.5 Fix Supabase Client Configuration

**Estimasi:** 1 jam | **Risiko:** Low | **Dependencies:** 2.2

#### File yang Diubah

| File | Aksi |
|------|------|
| `src/lib/supabaseClient.ts` | Tambah validasi env, retry, error handling |

#### Langkah Detail

**Step 1: Tambah validasi environment — throw error, bukan warn**

Saat ini `supabaseClient.ts` baris 7-9 hanya `console.warn`, tidak throw error. Ini berarti app tetap berjalan dengan client kosong dan akan fail silently di runtime.

```typescript
// SEBELUM (baris 7-9)
if (!supabaseUrl || !supabaseAnonKey) {
  console.warn('Supabase URL or Anon Key is missing in environment variables.')
}
// ... tetap create client dengan empty string

// SESUDAH
if (!supabaseUrl || !supabaseAnonKey) {
  throw new Error('Missing Supabase configuration. Check your .env file.')
}

export const supabase = createClient<Database>(
  supabaseUrl,
  supabaseAnonKey,
  {
    auth: {
      persistSession: true,
      autoRefreshToken: true,
      detectSessionInUrl: true,
      storage: localStorage,
      storageKey: 'kurirdev-auth-token',
    },
    realtime: {
      params: { eventsPerSecond: 10 },
    },
  }
)
```

**Step 2: Tambah auth state change handler**
```typescript
// Tambahkan di akhir file
supabase.auth.onAuthStateChange((event, session) => {
  if (event === 'SIGNED_OUT') {
    // Clear all stores
    useSessionStore.getState().reset()
    useUserStore.getState().reset()
  }
  if (event === 'TOKEN_REFRESHED') {
    // Token refreshed successfully
  }
  if (event === 'USER_UPDATED') {
    // Profile updated
  }
})
```

#### Testing
- [ ] Missing env vars → app throw error di startup
- [ ] Sign out dari Supabase → stores tereset
- [ ] Token refresh → session tetap valid

#### Acceptance Criteria
- ✅ Fail fast jika env vars missing
- ✅ Auth state change handler terpasang
- ✅ Stores reset otomatis saat sign out

---

## Phase 3 — Maintainability (Refactoring & Cleanup)

### 3.1 Hapus File Sampah (Dead Code)

**Estimasi:** 30 menit | **Risiko:** Low | **Dependencies:** Tidak ada

#### File yang Dihapus

| File | Alasan |
|------|--------|
| `src/services/api.ts` | Zero imports, REST API client untuk backend yang tidak ada |
| `src/services/mockData.ts` | Zero imports, data dummy tidak relevan |
| `src/components/layout/Sidebar.tsx` | Zero imports, branding lama "DeliveryPro" |
| `src/stores/useCourierStore.ts.bak` | Backup file, tidak dipakai |
| `src/stores/useUserStore.ts.bak` | Backup file, tidak dipakai |

#### Langkah Detail

**Step 1: Verifikasi tidak ada import**
```bash
# Sudah dikonfirmasi oleh agent — zero imports untuk ketiga file
```

**Step 2: Hapus file**
```bash
rm src/services/api.ts
rm src/services/mockData.ts
rm src/components/layout/Sidebar.tsx
rm src/stores/useCourierStore.ts.bak
rm src/stores/useUserStore.ts.bak
```

**Step 3: Verifikasi build masih sukses**
```bash
npm run build
```

#### Testing
- [ ] `npm run build` sukses tanpa error
- [ ] `npm run dev` berjalan normal
- [ ] Tidak ada import yang broken

#### Acceptance Criteria
- ✅ Semua file sampah terhapus
- ✅ Build sukses
- ✅ App berjalan normal

---

### 3.2 Pecah Orders.tsx (1952 baris)

**Estimasi:** 6 jam | **Risiko:** Medium | **Dependencies:** 3.1

#### Struktur Baru
```
src/pages/Orders.tsx (~200 baris, orchestrator)
├── src/components/orders/OrderList.tsx
├── src/components/orders/CreateOrderModal.tsx
├── src/components/orders/OrderDetailModal.tsx
├── src/components/orders/BulkSettlementModal.tsx
├── src/components/orders/AddressPicker.tsx
├── src/lib/exportOrders.ts (CSV export)
├── src/lib/printInvoice.ts (Invoice print)
└── src/hooks/useOrderFilters.ts
```

#### Langkah Detail

**Step 1: Extract utility functions ke lib**
```typescript
// src/lib/exportOrders.ts — ambil handleExportCSV dari Orders.tsx
export function exportOrdersToCSV(orders: Order[], couriers: Courier[], settings: Settings) {
  // ... pindahkan dari Orders.tsx L523-586
}

// src/lib/printInvoice.ts — ambil handlePrintInvoice dari Orders.tsx
export function printInvoice(order: Order, settings: Settings) {
  // ... pindahkan dari Orders.tsx L588-713
}

// src/lib/calcEarning.ts — tambahkan calcPlatformFee
export const calcPlatformFee = (order: Order, settings: EarningSettings): number => {
  const rate = order.applied_commission_rate ?? settings.commission_rate
  const threshold = order.applied_commission_threshold ?? settings.commission_threshold
  if (order.total_fee <= threshold) return 0
  return order.total_fee * (1 - rate / 100)
}
```

**Step 2: Extract custom hook**
```typescript
// src/hooks/useOrderFilters.ts
export function useOrderFilters(orders: Order[], search: string, filters: OrderFilters) {
  return useMemo(() => {
    // ... pindahkan filteredOrders logic dari Orders.tsx L223-287
  }, [orders, search, filters])
}
```

**Step 3: Extract komponen modal**
```typescript
// src/components/orders/CreateOrderModal.tsx
// ... pindahkan JSX modal create order dari Orders.tsx L1057-1346

// src/components/orders/OrderDetailModal.tsx
// ... pindahkan JSX detail/assign modal dari Orders.tsx L1349-1823

// src/components/orders/BulkSettlementModal.tsx
// ... pindahkan JSX bulk settlement dari Orders.tsx L1838-1949
```

**Step 4: Simplify Orders.tsx**
```typescript
// Orders.tsx sekarang hanya ~200 baris:
// - State declarations
// - Store hooks
// - Handler wrappers (delegasi ke child components)
// - Layout/orchestration
```

#### Testing
- [ ] Semua fitur Orders masih berfungsi setelah refactor
- [ ] Create order, assign, detail modal, CSV export, invoice print
- [ ] Bulk settlement masih berfungsi
- [ ] Filter dan search masih berfungsi

#### Acceptance Criteria
- ✅ Orders.tsx < 300 baris
- ✅ Semua fitur berfungsi sama seperti sebelumnya
- ✅ Setiap komponen baru < 300 baris
- ✅ TypeScript compile tanpa error

---

### 3.3 Pecah Settings.tsx (1330 baris)

**Estimasi:** 4 jam | **Risiko:** Medium | **Dependencies:** 3.1

#### Struktur Baru
```
src/pages/Settings.tsx (~150 baris, orchestrator)
├── src/components/settings/ProfileTab.tsx
├── src/components/settings/PasswordTab.tsx
├── src/components/settings/UsersTab.tsx
├── src/components/settings/BusinessTab.tsx
├── src/components/settings/InstructionsTab.tsx
├── src/components/settings/CacheManagement.tsx
├── src/components/settings/AddUserModal.tsx
├── src/components/settings/EditUserModal.tsx
└── src/lib/syncSettings.ts
```

#### Langkah Detail

**Step 1: Extract sync utility**
```typescript
// src/lib/syncSettings.ts
export async function syncSettingsToServer(settings: Settings) {
  // ... pindahkan dari Settings.tsx L62-76
}
```

**Step 2: Extract setiap tab sebagai komponen**
```typescript
// src/components/settings/ProfileTab.tsx
// ... pindahkan profile editing UI

// src/components/settings/UsersTab.tsx
// ... pindahkan user list + CRUD logic

// src/components/settings/BusinessTab.tsx
// ... pindahkan business settings + commission settings

// src/components/settings/InstructionsTab.tsx
// ... pindahkan courier instructions
```

**Step 3: Simplify Settings.tsx**
```typescript
// Settings.tsx sekarang hanya ~150 baris:
// - Tab state management
// - Store hooks
// - Tab navigation
// - Render tab components
```

#### Testing
- [ ] Semua 5 tab di Settings masih berfungsi
- [ ] User CRUD masih berfungsi
- [ ] Settings sync ke server masih berfungsi
- [ ] Cache management masih berfungsi

#### Acceptance Criteria
- ✅ Settings.tsx < 200 baris
- ✅ Semua tab berfungsi sama
- ✅ Setiap komponen baru < 250 baris

---

### 3.4 Pecah CourierOrderDetail.tsx (1052 baris)

**Estimasi:** 4 jam | **Risiko:** Medium | **Dependencies:** 3.1

#### Struktur Baru
```
src/pages/courier/CourierOrderDetail.tsx (~200 baris, orchestrator)
├── src/components/courier/AddressPicker.tsx
├── src/components/courier/ItemForm.tsx
├── src/components/courier/BiayaTambahan.tsx
├── src/components/courier/OrderTimeline.tsx
├── src/components/courier/CancelModal.tsx
└── src/lib/generateInvoiceHtml.ts
```

#### Langkah Detail

**Step 1: Extract invoice HTML generator**
```typescript
// src/lib/generateInvoiceHtml.ts
export function generateInvoiceHtml(order: Order, settings: Settings): string {
  // ... pindahkan dari CourierOrderDetail.tsx L948-1051
}
```

**Step 2: Extract komponen UI**
```typescript
// src/components/courier/AddressPicker.tsx
// ... pindahkan address picker UI

// src/components/courier/ItemForm.tsx
// ... pindahkan item management UI

// src/components/courier/BiayaTambahan.tsx
// ... pindahkan titik & beban UI

// src/components/courier/OrderTimeline.tsx
// ... pindahkan timeline UI
```

#### Testing
- [ ] Order detail page masih berfungsi
- [ ] Customer edit, address picker, item management
- [ ] Biaya tambahan (titik & beban)
- [ ] Invoice generation & share
- [ ] Cancel order flow

#### Acceptance Criteria
- ✅ CourierOrderDetail.tsx < 250 baris
- ✅ Semua fitur berfungsi sama
- ✅ Setiap komponen baru < 200 baris

---

### 3.5 Fix Reports.tsx — Duplicated Commission Calculation

**Estimasi:** 1 jam | **Risiko:** Low | **Dependencies:** Tidak ada

#### File yang Diubah

| File | Aksi |
|------|------|
| `src/pages/Reports.tsx` | Gunakan `calcAdminEarning` dari calcEarning.ts |
| `src/lib/calcEarning.ts` | Pastikan export lengkap |

#### Langkah Detail

**Step 1: Ganti inline commission calc di Reports.tsx:188-193**
```typescript
// SEBELUM
deliveredOrders.forEach(o => {
  const rate = commission_rate / 100;
  totalCourierPayout += (o.total_fee || 0) * rate;
});

// SESUDAH
import { calcAdminEarning, calcCourierEarning } from '@/lib/calcEarning'

deliveredOrders.forEach(o => {
  const { adminEarning, courierEarning } = calcAdminEarning(o, {
    commission_rate,
    commission_threshold,
  })
  totalCourierPayout += courierEarning
  totalAdminRevenue += adminEarning
})
```

**Catatan Penting:** Gunakan `order.applied_commission_rate` (rate saat order dibuat), bukan `commission_rate` (rate saat ini). Ini mencegah laporan salah jika rate pernah berubah.

#### Testing
- [ ] Reports menampilkan angka yang sama sebelum dan sesudah refactor
- [ ] Ubah commission rate → laporan historis tidak berubah
- [ ] PDF export masih berfungsi

#### Acceptance Criteria
- ✅ Tidak ada inline commission calculation
- ✅ Menggunakan `calcAdminEarning`/`calcCourierEarning`
- ✅ Menggunakan `applied_commission_rate` per order

---

### 3.6 Setup Testing Infrastructure

**Estimasi:** 4 jam | **Risiko:** Low | **Dependencies:** 3.1 (hapus dead code dulu)

#### File yang Dibuat/Diubah

| File | Aksi |
|------|------|
| `package.json` | Tambah Vitest + Testing Library |
| `vitest.config.ts` | Buat baru |
| `src/__tests__/setup.ts` | Buat baru |
| `src/__tests__/calcEarning.test.ts` | Test pertama |

#### Langkah Detail

**Step 1: Install dependencies**
```bash
npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom happy-dom
```

**Step 2: Buat `vitest.config.ts`**
```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  test: {
    environment: 'jsdom',
    setupFiles: ['./src/__tests__/setup.ts'],
    globals: true,
    include: ['src/**/*.{test,spec}.{ts,tsx}'],
  },
})
```

**Step 3: Buat `src/__tests__/setup.ts`**
```typescript
import '@testing-library/jest-dom'
```

**Step 4: Tambah script ke `package.json`**
```json
{
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  }
}
```

**Step 5: Buat test pertama — `calcEarning.test.ts`**
```typescript
import { describe, it, expect } from 'vitest'
import { calcCourierEarning, calcAdminEarning } from '@/lib/calcEarning'

describe('calcCourierEarning', () => {
  it('should calculate courier earning correctly', () => {
    const order = { total_fee: 100000, applied_commission_rate: 20 }
    const result = calcCourierEarning(order, { commission_rate: 20, commission_threshold: 0 })
    expect(result.courierEarning).toBe(20000)
  })

  it('should return 0 if fee below threshold', () => {
    // ...
  })
})

describe('calcAdminEarning', () => {
  it('should calculate admin earning correctly', () => {
    // ...
  })
})
```

#### Testing
- [ ] `npm run test` menjalankan semua test
- [ ] `calcEarning.test.ts` pass
- [ ] Coverage report generate

#### Acceptance Criteria
- ✅ Vitest terkonfigurasi
- ✅ Minimal 1 test file (calcEarning)
- ✅ `npm run test` berjalan sukses

---

## Phase 4 — Polish (Optimasi)

### 4.1 Fix Dashboard Trend Data

**Estimasi:** 30 menit | **Risiko:** Low

#### File yang Diubah

| File | Aksi |
|------|------|
| `src/pages/Dashboard.tsx` | Hitung trend dari data real |

#### Langkah Detail

**Step 1: Hitung trend dari perbandingan data**
```typescript
// Ganti hardcoded trend dengan kalkulasi real
const todayRevenue = // ... sum revenue hari ini
const yesterdayRevenue = // ... sum revenue kemarin
const trend = yesterdayRevenue > 0
  ? ((todayRevenue - yesterdayRevenue) / yesterdayRevenue) * 100
  : 0
```

---

### 4.2 Fix Timezone di IndexedDB

**Estimasi:** 2 jam | **Risiko:** Medium

#### File yang Diubah

| File | Aksi |
|------|------|
| `src/lib/orderCache.ts` | Gunakan UTC secara konsisten |

#### Langkah Detail

**Step 1: Fix `getLocalDateStr`**
```typescript
// SEBELUM — menggunakan local date
function getLocalDateStr(isoString: string): string {
  const date = new Date(isoString)
  return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, '0')}-${String(date.getDate()).padStart(2, '0')}`
}

// SESUDAH — menggunakan UTC
function getUTCDateStr(isoString: string): string {
  const date = new Date(isoString)
  return `${date.getUTCFullYear()}-${String(date.getUTCMonth() + 1).padStart(2, '0')}-${String(date.getUTCDate()).padStart(2, '0')}`
}
```

**Step 2: Update semua date range queries untuk menggunakan UTC**

---

### 4.3 Tambah IndexedDB Auto-Cleanup

**Estimasi:** 2 jam | **Risiko:** Low

#### File yang Diubah

| File | Aksi |
|------|------|
| `src/lib/orderCache.ts` | Tambah prune mechanism |

#### Langkah Detail

**Step 1: Tambah fungsi prune**
```typescript
export async function pruneOldOrders(maxAgeDays: number = 365): Promise<number> {
  const cutoffDate = new Date()
  cutoffDate.setDate(cutoffDate.getDate() - maxAgeDays)

  const oldOrders = await localDB.orders
    .where('created_at')
    .below(cutoffDate.toISOString())
    .toArray()

  await localDB.orders.bulkDelete(oldOrders.map(o => o.id))

  // Update metadata
  const total = await localDB.orders.count()
  saveMeta({ total_records: total })

  return oldOrders.length
}
```

**Step 2: Panggil saat app start (di AppListeners)**
```typescript
// Cek storage quota dan prune jika perlu
const estimate = await navigator.storage?.estimate()
if (estimate && estimate.usage && estimate.quota) {
  const usagePercent = (estimate.usage / estimate.quota) * 100
  if (usagePercent > 80) {
    await pruneOldOrders(180) // Prune lebih agresif jika storage hampir penuh
  }
}
```

---

### 4.4 Tambah Logging Utility

**Estimasi:** 1 jam | **Risiko:** Low

#### File yang Dibuat/Diubah

| File | Aksi |
|------|------|
| `src/lib/logger.ts` | Buat baru |
| Semua file dengan console.log | Ganti dengan logger |

#### Langkah Detail

**Step 1: Buat `logger.ts`**
```typescript
const isDev = import.meta.env.DEV

export const logger = {
  debug: (...args: unknown[]) => isDev && console.log('[DEBUG]', ...args),
  info: (...args: unknown[]) => console.info('[INFO]', ...args),
  warn: (...args: unknown[]) => console.warn('[WARN]', ...args),
  error: (...args: unknown[]) => console.error('[ERROR]', ...args),
}
```

**Step 2: Ganti semua `console.log` di production files**

Files yang perlu diupdate:
- `src/lib/firebase.ts`
- `src/lib/fcm.ts`
- `src/context/AuthContext.tsx`
- `src/pages/Login.tsx`
- `src/stores/*.ts`

---

## Testing Strategy

### Unit Tests (Priority Order)

| Priority | Module | Test Cases |
|----------|--------|------------|
| 1 | `calcEarning.ts` | Commission calc, threshold, edge cases |
| 2 | `retry.ts` | Retry logic, exponential backoff, non-retryable errors |
| 3 | `generateOrderId` | Uniqueness, date formatting, fallback |
| 4 | Order status transitions | Valid state changes |
| 5 | Auth flow | Login, logout, suspend check |

### Integration Tests

| Test | Description |
|------|-------------|
| Order CRUD flow | Create → Assign → Deliver → Complete |
| User management | Create user → Suspend → Login rejected |
| Settings sync | Change settings → Sync → Verify |

### E2E Tests (Playwright — Future)

| Test | Description |
|------|-------------|
| Admin creates order | Full flow dari login admin sampai order created |
| Courier delivers order | Full flow dari login courier sampai order delivered |
| Offline scenario | Simulate offline → update status → verify retry |

---

## Rollback Plan

### Per-Phase Rollback

Setiap phase di-commit secara terpisah. Jika ada masalah:

```bash
# Rollback ke commit sebelumnya
git revert HEAD

# Atau checkout branch backup
git checkout backup-before-phase-1
```

### Database Rollback

Setiap migration SQL harus reversible:
```sql
-- UP
ALTER TABLE orders ADD CONSTRAINT unique_order_number UNIQUE (order_number);

-- DOWN
ALTER TABLE orders DROP CONSTRAINT IF EXISTS unique_order_number;
```

### Feature Flags

Untuk perubahan besar (seperti refactoring Orders.tsx), gunakan feature flag:
```typescript
const ENABLE_NEW_ORDERS_UI = import.meta.env.VITE_ENABLE_NEW_ORDERS_UI === 'true'
```

---

## Timeline Estimasi

| Phase | Estimasi | Target |
|-------|----------|--------|
| Phase 1 (Security) | ~8 jam | Minggu 1 |
| Phase 2 (Reliability) | ~11 jam | Minggu 2-3 |
| Phase 3 (Maintainability) | ~20 jam | Minggu 3-4 |
| Phase 4 (Polish) | ~6 jam | Week 5+ |
| **Total** | **~45 jam** | **~5 minggu** |

---

## Checklist Sebelum Mulai

- [ ] Backup database Supabase
- [ ] Buat branch `phase-1-security` dari `main`
- [ ] Verifikasi semua env vars ada di `.env.local`
- [ ] Pastikan `.env` tidak ter-commit (cek `.gitignore`)
- [ ] Informasikan tim tentang planned changes
- [ ] Siapkan testing environment

---

> **Dibuat:** 3 April 2026
> **Status:** Ready for Implementation
> **Next Step:** Mulai Phase 1 — Security Fixes

---

## 📝 Revisi Setelah Review Ulang

Berikut temuan tambahan yang terlewat dari analisa awal dan sudah ditambahkan ke plan:

### Temuan Baru yang Ditambahkan:

| # | Temuan | Lokasi | Severity | Status |
|---|--------|--------|----------|--------|
| 1 | **CourierProfile ganti password client-side** | `CourierProfile.tsx:64-94` | 🔴 KRITIS | ✅ Ditambahkan di 1.1 Step 6 |
| 2 | **Default password tampil di UI Login** | `Login.tsx:279` | 🟠 Menengah | ✅ Ditambahkan di 1.1 Step 7 |
| 3 | **Console.log info leak di AuthContext** | `AuthContext.tsx:95,107,122,147` | 🟡 Kecil | ✅ Ditambahkan sebagai section 1.6 |
| 4 | **Notification service endpoint tidak ada** | `notificationService.ts:10` | 🟠 Menengah | ✅ Diperbarui di 1.5 dengan rewrite lengkap |
| 5 | **Queue manipulasi di 5 fungsi courier store** | `useCourierStore.ts:94,98,113,142,80` | 🟠 Menengah | ✅ Diperbarui di 2.4 dengan rewrite lengkap |
| 6 | **Persist middleware menyimpan password** | `useCourierStore.ts:70-205` | 🔴 KRITIS | ✅ Diperbarui di 2.4 — hapus persist |

### Yang Sudah Diverifikasi dan TIDAK Bermasalah:

| Item | Status |
|------|--------|
| `useUserStore.ts` `addUser` — password dikirim ke edge function | ✅ Sudah benar (tidak disimpan di state) |
| `AuthContext.tsx` `fetchProfile` — `is_active` dari database | ✅ Sudah benar (`profile.is_active ?? true`) |
| `useCourierStore.ts` `couriers` getter — dari `useUserStore` | ✅ Sudah benar |
| `useCourierStore.ts` `getAvailableCouriers` — dari `useUserStore` | ✅ Sudah benar |
| `useCourierStore.ts` `rotateQueue` — queue_position dari `useUserStore` | ✅ Sudah benar (tapi queue lokal tetap dimanipulasi) |
| `.env` tidak ada `VITE_API_SECRET` | ✅ Sudah dikonfirmasi — tidak ada |

---

## 📝 Revisi Setelah Review Ulang

Berikut temuan tambahan yang terlewat dari analisa awal dan sudah ditambahkan ke plan:

### Temuan Baru yang Ditambahkan:

| # | Temuan | Lokasi | Severity | Status |
|---|--------|--------|----------|--------|
| 1 | **CourierProfile ganti password client-side** | `CourierProfile.tsx:64-94` | 🔴 KRITIS | ✅ Ditambahkan di 1.1 Step 6 |
| 2 | **Default password tampil di UI Login** | `Login.tsx:279` | 🟠 Menengah | ✅ Ditambahkan di 1.1 Step 7 |
| 3 | **Console.log info leak di AuthContext** | `AuthContext.tsx:95,107,122,147` | 🟡 Kecil | ✅ Ditambahkan sebagai section 1.6 |
| 4 | **Notification service endpoint tidak ada** | `notificationService.ts:10` | 🟠 Menengah | ✅ Diperbarui di 1.5 dengan rewrite lengkap |
| 5 | **Queue manipulasi di 5 fungsi courier store** | `useCourierStore.ts:94,98,113,142,80` | 🟠 Menengah | ✅ Diperbarui di 2.4 dengan rewrite lengkap |
| 6 | **Persist middleware menyimpan password** | `useCourierStore.ts:70-205` | 🔴 KRITIS | ✅ Diperbarui di 2.4 — hapus persist |

### Yang Sudah Diverifikasi dan TIDAK Bermasalah:

| Item | Status |
|------|--------|
| `useUserStore.ts` `addUser` — password dikirim ke edge function | ✅ Sudah benar (tidak disimpan di state) |
| `AuthContext.tsx` `fetchProfile` — `is_active` dari database | ✅ Sudah benar (`profile.is_active ?? true`) |
| `useCourierStore.ts` `couriers` getter — dari `useUserStore` | ✅ Sudah benar |
| `useCourierStore.ts` `getAvailableCouriers` — dari `useUserStore` | ✅ Sudah benar |
| `useCourierStore.ts` `rotateQueue` — queue_position dari `useUserStore` | ✅ Sudah benar (tapi queue lokal tetap dimanipulasi) |
| `.env` tidak ada `VITE_API_SECRET` | ✅ Sudah dikonfirmasi — tidak ada |
